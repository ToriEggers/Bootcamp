## FIU Genomics & Bioinformatics Analysis CURE BSC3990L Genomics ###

***Multiple Sequence Alignment and Phylogenomics tutorial***

A phylogeny can be built from one sequence (like the COI or 16s), or it can include several genes, like all variants of the 16s (in ecoli there are 7: https://biocyc.org/gene?orgid=ECOLI&id=EG30085), or all the shared single orthologous genes (BUSCO) for example. Regardless, the foundation of all phylogenies is a multiple sequence alignment, where we correlate the number of sequence mismatches and gaps to evolutionary distance. Many softwares and pipelines exist to accomplish this. Here we use mafft, clipkit, and iqtree. At the end are two methods of viewing your tree once it is generated: Jalview and RStudio.

<details>
  <summary><b>Single gene</b></summary>

The first step is to gather the gene sequence you are interested in aligning. We will use the rrsA 16s sequence as a continuation from the [blast tutorial](). If you have completed this tutorial, then you should have a directory called blast_loop with 5 ecoli genomes and the directory called blast_out. **cd to blast_out** and you should have 5 output files that loop like:
```
```

**Note: ** For nematodes, the ribosomal gene typically used for phylogenies is the 18s sequence (it's actually 18s, 5.8s, and 28s of the rDNA, but we'll say 18s for simplicity) taken from a previous nematode phylogenomics paper: https://www.pnas.org/doi/epdf/10.1073/pnas.0403094101 The nematode 18s sequence is in the file /home/data/jfierst_classroom/msaPractice/18s.fasta

The second step is to find that sequence of interest (here, 18s) in all the genomes of the organisms you want in your phylogeny. The [BLAST](https://github.com/ToriEggers/Bootcamp/blob/main/5.ntBLAST.md) tutorial may help you out with this.

::::::::::::::
msa.sh
::::::::::::::
#!/bin/bash

#SBATCH --account iacc_jfierst
#SBATCH --qos highmem1
#SBATCH --partition highmem1
#SBATCH --output=out_18s_msa.log

#for file in ./blast_out/*; do
#  cat $file | head -1 | cut -f 6 > ${file}.temp
#  sed -i 's/-//g' ${file}.temp
#  mv ${file}.temp ${file}
#done

#fix > in fastas so it is the strain names
#for file in ./blast_out/*; do
#  filename=$(basename $file)
#  { echo ">"$filename; cat $file; } > temp && mv temp $file
#  sed -i 's/_blast.out//g' $file
#done

# msa for each gene
#module load mafft-7.221-gcc-8.2.0-y6cgezm

#mafft --localpair --maxiterate 1000 ./blast_out/cat_18s.fasta > 18s_aligned.fasta

#linsi --thread 16 --maxiterate 1000 ./fasta/cat_16s.fasta > 16s_aligned.fasta

# trim alignments
module load mamba/23.1.0-4

source activate clipkit

clipkit -m smart-gap 18s_aligned.fasta

# iqtree
module load iqtree-2-gcc-8.2.0

iqtree2 -s 18s_aligned.fasta.clipkit -m MFP -bb 1000 -alrt 1000 -nt AUTO


  
</details>

<details>
  <summary><b>Multi-gene</b></summary>
</details>

<details>
  <summary><b>Visualization</b></summary>
</details>

